export default function Offline() {
  return <main id="main-content" className="offline" tabIndex={-1}><p className="eyebrow">TRADEARENA</p><h1 tabIndex={-1}>Sin conexión / Offline</h1><p>Vuelve a conectarte para acceder a tu arena privada. Reconnect to access your private arena.</p></main>;
}
